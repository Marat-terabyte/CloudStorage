<UserControl x:Class="Client.View.SignInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.View" xmlns:viewmodels="clr-namespace:Client.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock FontSize="28">Sign in</TextBlock>
            <TextBlock>Username</TextBlock>
            <TextBox Height="28" Width="350" FontSize="18" Text="{Binding User.Username}"/>
            <TextBlock>Password</TextBlock>
            <TextBox Height="28" Width="350" FontSize="18" Text="{Binding User.Password}"/>
            <Button Command="{Binding SignInCommand}">Sign in</Button>
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <TextBlock>Don't Have an Account?</TextBlock>
                <Button FontSize="12" Command="{Binding GoToSignUp}">Sign up</Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
